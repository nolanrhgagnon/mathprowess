global:
  scrape_interval: 15s

scrap_configs:
  - job_name: "prometheus"
    static_configs:
      - targets: ["node-exporter:9090"]

  - job_name: "node"
      static_configs:
        - targets: ["node_exporter:9100"]
